public class java-variables {

    static void Exercise(String[] args) {

        // Exercise 1: Create a variable named carName and assign the value Volvo to it.
        String carName = "Volvo";

        // Exercise 2: Create a variable named maxSpeed and assign the value 120 to it.
        int maxSpeed = 120;

        // Exercise 3: Display the sum of 5 + 10, using two variables: x and y.
        int x = 5, y = 10;
        System.out.println(x + y);

        // Exwercise 4: Create a variable called z, assign x + y to it, and display the result.
        int z = x + y;
        System.out.println(z);
    }
}